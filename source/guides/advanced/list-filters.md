Loop Optimization: List Filters
-------------------------------

**TODO: Write-up that explains tags like `list.filter`, `list.parse`, `list.sort_by_number` and why this is better than a foreach loop.**
